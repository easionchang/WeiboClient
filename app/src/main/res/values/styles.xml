<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/primary</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:textColorPrimary">@color/icons</item>
        <!--Window color-->
        <item name="android:windowBackground">@color/dim_foreground_material_dark</item>
        <item name="textAppearanceLargePopupMenu">@style/PopupMenu.MyAppTheme</item>

        <item name="searchViewStyle">@style/Serchview.MyAppTheme</item>
    </style>


    <style name="PopupMenu.MyAppTheme" parent="TextAppearance.AppCompat.Light.Widget.PopupMenu.Small">
        <item name="android:textColor">#000</item>
    </style>

    <style name="Serchview.MyAppTheme" parent="Widget.AppCompat.Light.SearchView">
        <item name="closeIcon">@drawable/abc_btn_radio_to_on_mtrl_000</item>
    </style>
</resources>
